# ── macOS Apple Silicon (M1–M5) ───────────────────────────────────────────────
sounddevice
pynput
pyperclip
numpy
mlx-whisper

# ── System-tray UI (all platforms) ────────────────────────────────────────────
pystray
Pillow

# ── macOS Intel / Linux / Windows — replace mlx-whisper with: ─────────────────
# faster-whisper

# ── OpenAI API backend — replace mlx-whisper with: ────────────────────────────
# openai

# Or run the universal installer — it picks the right packages automatically:
# python3 whisper_install.py
